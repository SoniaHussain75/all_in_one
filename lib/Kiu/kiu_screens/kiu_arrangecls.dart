import 'package:flutter/material.dart';
class KiuArrangecls extends StatelessWidget {
  const KiuArrangecls({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Arrange Classes"),
      ),
    );
  }
}
