import 'package:flutter/material.dart';
class KiuActivecourse extends StatelessWidget {
  const KiuActivecourse({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Active Course "),
      ),
    );
  }
}
