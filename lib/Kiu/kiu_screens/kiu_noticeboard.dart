import 'package:flutter/material.dart';
class KiuNoticeboard extends StatelessWidget {
  const KiuNoticeboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Notice board"),
      ),

    );

  }
}
