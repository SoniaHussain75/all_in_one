import 'package:flutter/material.dart';
class KiuLibrary extends StatelessWidget {
  const KiuLibrary({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Library"),
      ),
    );
  }
}
