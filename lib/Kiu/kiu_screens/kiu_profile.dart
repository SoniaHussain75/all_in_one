import 'package:flutter/material.dart';
class KiuProfile extends StatelessWidget {
  const KiuProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Profile"),
      ),
    );
  }
}
