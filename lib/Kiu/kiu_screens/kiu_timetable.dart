import 'package:flutter/material.dart';
class KiuTimetable extends StatelessWidget {
  const KiuTimetable({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("TimeTable"),
      ),
    );
  }
}
