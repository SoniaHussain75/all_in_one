import 'package:flutter/material.dart';
class KiuLogout extends StatelessWidget {
  const KiuLogout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Logout"),
      ),
    );
  }
}
