import 'package:flutter/material.dart';
class KiuTransport extends StatelessWidget {
  const KiuTransport({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
       appBar: AppBar(
         title: Text("transport screen "),

       ),
    );
  }
}
