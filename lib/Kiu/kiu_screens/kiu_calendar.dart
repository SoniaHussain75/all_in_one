import 'package:flutter/material.dart';
class KiuCalendar extends StatelessWidget {
  const KiuCalendar({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Calendar"),
      ),
    );
  }
}
